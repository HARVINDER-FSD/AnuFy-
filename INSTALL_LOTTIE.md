# Install Lottie for Animated Emojis

Run this command to install Lottie:

```bash
npm install lottie-react
```

Or with yarn:

```bash
yarn add lottie-react
```

This will enable animated emoji reactions in the chat!
